apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: roboshop
spec:
  selector:
    # labels are used for selector
    project: roboshop
    tier: db
    component: redis
  ports:
    - protocol: TCP
      port: 6379 # service port
      targetPort: 6379 # container port